# cdscli Example

Display help for the Chaos Data Service CLI.

**Before**: You may not know the options for managing CDS files.

**After**: The command prints usage instructions so you can explore features.

Run:
```bash
./run.sh
```
