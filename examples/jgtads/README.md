# jgtads Example

Plot ADS analytics from a sample CDS file.

**Before**: You have a CDS file but no visual analysis.

**After**: Running the script (just `jgtads --help` here) shows how to invoke the
tool so you can produce ADS charts.

Run:
```bash
./run.sh
```
