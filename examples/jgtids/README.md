# jgtids Example

Generate an IDS file from a sample CDS input.

**Before**: You have market data but no IDS indicators.

**After**: Running `jgtids --help` shows the options for creating those IDS fil
es.

Run:
```bash
./run.sh
```
