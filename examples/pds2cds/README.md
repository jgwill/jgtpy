# pds2cds Example

Convert a sample PDS file to CDS format.

The script uses a bundled sample from `samples/`.

**Before**: You only have the PDS file `SPX500_H1_240229.pds.full.csv`.

**After**: A new file named `output.cds.csv` is created in this directory.

Run:
```bash
./run.sh
```
