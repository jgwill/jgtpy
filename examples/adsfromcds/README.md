# adsfromcds Example

Create charts from cached CDS data.

**Before**: You have CDS cache files but no generated charts.

**After**: The command (`adsfromcds --help` here) shows how to produce charts f
rom those caches.

Run:
```bash
./run.sh
```
